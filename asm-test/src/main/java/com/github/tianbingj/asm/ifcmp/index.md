## 条件分支

生成if else相关代码
```$xslt
实现一个类，添加方法：

class ExerciseIf {

    public static int judge(int args) {
        if (args > 0) {
            return 1;
        } else if (args == 0) {
            return 0;
        } else {
            return -1;
        }
    }
}

```